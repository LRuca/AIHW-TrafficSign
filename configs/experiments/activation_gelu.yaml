experiment_name: stage2_activation_gelu
metadata:
  stage: activation
  dimension: model.activation
  value: gelu
model:
  name: mobilenet_v2
  pretrained: true
  activation: gelu
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
