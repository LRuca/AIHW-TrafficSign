experiment_name: stage4_augmentation_basic_color
metadata:
  stage: augmentation
  dimension: augmentation.policy
  value: basic_color
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
augmentation:
  policy: basic_color
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
