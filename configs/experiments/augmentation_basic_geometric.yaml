experiment_name: stage4_augmentation_basic_geometric
metadata:
  stage: augmentation
  dimension: augmentation.policy
  value: basic_geometric
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
augmentation:
  policy: basic_geometric
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
