experiment_name: stage4_augmentation_none
metadata:
  stage: augmentation
  dimension: augmentation.policy
  value: none
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
augmentation:
  policy: none
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
