experiment_name: stage11_dropout_01
metadata:
  stage: dropout
  dimension: model.dropout
  value: 0.1
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
  dropout: 0.1
finetune:
  mode: freeze_then_full
  freeze_epochs: 5

