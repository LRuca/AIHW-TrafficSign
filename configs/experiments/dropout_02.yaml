experiment_name: stage11_dropout_02
metadata:
  stage: dropout
  dimension: model.dropout
  value: 0.2
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
  dropout: 0.2
finetune:
  mode: freeze_then_full
  freeze_epochs: 5

