experiment_name: stage11_dropout_03
metadata:
  stage: dropout
  dimension: model.dropout
  value: 0.3
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
  dropout: 0.3
finetune:
  mode: freeze_then_full
  freeze_epochs: 5

