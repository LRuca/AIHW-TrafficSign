experiment_name: stage7_epochs_20
metadata:
  stage: epochs
  dimension: train.epochs
  value: 20
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
train:
  epochs: 20
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
