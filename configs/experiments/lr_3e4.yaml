experiment_name: stage9_lr_3e4
metadata:
  stage: learning_rate
  dimension: optimizer.lr
  value: 0.0003
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
optimizer:
  name: adamw
  lr: 0.0003
finetune:
  mode: freeze_then_full
  freeze_epochs: 5

