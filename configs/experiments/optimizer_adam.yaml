experiment_name: stage5_optimizer_adam
metadata:
  stage: optimizer
  dimension: optimizer.name
  value: adam
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
optimizer:
  name: adam
  lr: 0.001
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
