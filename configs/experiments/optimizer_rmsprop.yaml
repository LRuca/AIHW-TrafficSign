experiment_name: stage5_optimizer_rmsprop
metadata:
  stage: optimizer
  dimension: optimizer.name
  value: rmsprop
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
optimizer:
  name: rmsprop
  lr: 0.001
  momentum: 0.9
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
