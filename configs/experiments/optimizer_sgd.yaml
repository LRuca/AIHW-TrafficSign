experiment_name: stage5_optimizer_sgd
metadata:
  stage: optimizer
  dimension: optimizer.name
  value: sgd
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
optimizer:
  name: sgd
  lr: 0.01
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
