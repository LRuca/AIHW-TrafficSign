experiment_name: stage3_resolution_96
metadata:
  stage: resolution
  dimension: data.image_size
  value: 96
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
data:
  image_size: 96
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
