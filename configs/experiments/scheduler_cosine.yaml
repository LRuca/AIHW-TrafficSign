experiment_name: stage6_scheduler_cosine
metadata:
  stage: scheduler
  dimension: scheduler.name
  value: cosine
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
scheduler:
  name: cosine
optimizer:
  name: adamw
  lr: 0.001
finetune:
  mode: freeze_then_full
  freeze_epochs: 5
