experiment_name: stage10_weight_decay_1e5
metadata:
  stage: weight_decay
  dimension: optimizer.weight_decay
  value: 0.00001
model:
  name: mobilenet_v2
  pretrained: true
  activation: relu
optimizer:
  name: adamw
  lr: 0.001
  weight_decay: 0.00001
finetune:
  mode: freeze_then_full
  freeze_epochs: 5

